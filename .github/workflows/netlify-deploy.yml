name: Netlify Deploy

on:
  push:
    branches:
      - master
      - main

concurrency:
  group: netlify-production
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate Multiplayer WebSocket URL
        run: |
          if [ -z "$VITE_WS_URL" ]; then
            echo "Missing VITE_WS_URL secret. Set it to your production WebSocket server URL (wss://...)."
            exit 1
          fi
          if [[ "$VITE_WS_URL" != wss://* ]]; then
            echo "VITE_WS_URL must start with wss:// for production."
            exit 1
          fi
          if [ -z "$VITE_WORLD_ID" ]; then
            echo "Missing VITE_WORLD_ID secret. Set it to your canonical world ID (e.g. global-1)."
            exit 1
          fi
        env:
          VITE_WS_URL: ${{ secrets.VITE_WS_URL }}
          VITE_WORLD_ID: ${{ secrets.VITE_WORLD_ID }}

      - name: Build
        run: pnpm run build
        env:
          VITE_WS_URL: ${{ secrets.VITE_WS_URL }}
          VITE_WORLD_ID: ${{ secrets.VITE_WORLD_ID }}

      - name: Deploy to Netlify (Production)
        run: pnpm --package=netlify-cli dlx netlify deploy --dir=dist --prod --site=${{ secrets.NETLIFY_SITE_ID }}
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
